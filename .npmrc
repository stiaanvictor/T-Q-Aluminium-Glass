; The project folder name contains "&", which cmd.exe (npm's default script
; shell on Windows) treats as a command separator and breaks path resolution.
; Use PowerShell to run npm scripts so the quoted path is handled correctly.
script-shell=powershell.exe
